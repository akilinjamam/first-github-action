type AwsMediaFileService = (data: any) => any;

const postAwsMediaFileService: AwsMediaFileService = async (data) => {
  const result = data;
  return result;
};

export const awsService = {
  postAwsMediaFileService,
};
